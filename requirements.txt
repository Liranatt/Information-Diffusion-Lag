numpy>=2.0
pandas>=2.0
matplotlib>=3.8
asyncpg>=0.29
python-dotenv>=1.0
httpx>=0.27
pydantic>=2.0
scipy>=1.12
scikit-learn>=1.4
joblib>=1.3
numba>=0.60
# ibapi>=10.19 is IBKR's TWS-API-installer package (not on PyPI, which only has
# 9.81). Nothing in this repo imports it -- all IB access goes through ib_async
# -- so it is intentionally omitted here to keep `pip install` self-contained.
ib_async>=1.0